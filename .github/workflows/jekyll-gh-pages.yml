name: Deploy (Deprecated Jekyll Workflow)

on:
  workflow_dispatch:

on_disabled:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow
        run: echo "This workflow is deprecated. Use deploy.yml for GitHub Pages."
