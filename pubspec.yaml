name: frs_freies_radio_vorschau
description: Freies Radio Stuttgart – Premium App
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  html: ^0.15.4
  audio_service: ^0.18.13
  just_audio: ^0.10.5
  provider: ^6.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/logo/
